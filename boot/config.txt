[pi4]
# Disables dynamic frequency clocking
force_turbo=1
# uncomment if hdmi display is not detected and composite is being output
hdmi_force_hotplug=1
# Enable OTG
#OTG_CONFIG#
# Swap back the ttyAMA0 to GPIO
dtoverlay=miniuart-bt
# Overclocking settings
arm_freq=1750
gpu_freq=750
over_voltage=6

[all]
#NO_ZYNTHIAN_UPDATE
kernel=vmlinuz-6.1.54-llat-v7l+
os_prefix=6.1.54-llat-v7l+/
overlay_prefix=overlays/
# Disable Splash Screen
disable_splash=1
# Uncomment some or all of these to enable the optional hardware interfaces
dtparam=i2c_arm=on
dtparam=i2s=on
dtparam=spi=on
# Enable MMAP support (Jackd, etc.)
dtoverlay=i2s-mmap
# Enable UART for MIDI
enable_uart=1
dtoverlay=midi-uart0

# Enable RBPi Onboard Audio (Headphones)
dtparam=audio=on
audio_pwm_mode=2

initramfs initramfs.img
